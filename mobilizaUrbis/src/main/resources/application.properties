spring.application.name=mobilizaUrbis

# ===============================================
# CONFIGURA��O DO BANCO DE DADOS (DATASOURCE)
# ===============================================

# URL de conex�o JDBC para o PostgreSQL.
# Formato: jdbc:postgresql://<host>:<porta>/<nome_do_banco>
spring.datasource.url=jdbc:postgresql://localhost:5433/mobilizaUrbis

# Usu�rio e senha do seu banco de dados local.
# TROQUE PELOS SEUS DADOS! O usu�rio padr�o do Postgres geralmente � "postgres".
spring.datasource.username=postgres
spring.datasource.password=wellington


# ===============================================
# CONFIGURA��O DO JPA/HIBERNATE
# ===============================================

# Define como o Hibernate gerencia o esquema do banco.
# "update": Tenta atualizar o banco para que corresponda �s suas classes @Entity.
# Perfeito para desenvolvimento. N�o use em produ��o!
spring.jpa.hibernate.ddl-auto=update

# Mostra no console o SQL que o Hibernate est� executando.
# Excelente para depura��o e para aprender SQL.
spring.jpa.show-sql=true

# Formata o SQL exibido no console para ser mais leg�vel.
spring.jpa.properties.hibernate.format_sql=true

server.port=8081